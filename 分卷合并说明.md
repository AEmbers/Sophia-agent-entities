# 成员素材包 · 分卷合并说明

本目录下的 `成员素材.zip.001` ~ `成员素材.zip.005` 是同一个压缩包的**二进制分卷**，
因为 GitHub Contents API 单文件上限（约 20MB）限制，原始 88.2MB 的包被切分为 5 片。

## 合并方法

**macOS / Linux**

```bash
cat 成员素材.zip.001 成员素材.zip.002 成员素材.zip.003 成员素材.zip.004 成员素材.zip.005 > 成员素材.zip
```

**Windows（PowerShell）**

```powershell
cmd /c copy /b 成员素材.zip.001+成员素材.zip.002+成员素材.zip.003+成员素材.zip.004+成员素材.zip.005 成员素材.zip
```

**Windows（命令行，需 7-Zip）**

```cmd
7z x 成员素材.zip.001
```

## 校验

合并后的文件 SHA256 应为：

```
25ed812642bad2ee9d4b33d8c97225584a3a63d10c4d96a4d3a155e5bf003d7a
```

校验命令：

```bash
# macOS / Linux
shasum -a 256 成员素材.zip

# Windows PowerShell
Get-FileHash 成员素材.zip -Algorithm SHA256
```

## 各分片信息

| 分片 | 大小（字节） |
| --- | --- |
| 成员素材.zip.001 | 18,874,368 |
| 成员素材.zip.002 | 18,874,368 |
| 成员素材.zip.003 | 18,874,368 |
| 成员素材.zip.004 | 18,874,368 |
| 成员素材.zip.005 | 17,037,236 |
| **合计** | **92,534,708** |

## 包内内容

合并解压后得到 `成员素材/` 目录，包含：

- `readme.md` — 素材包说明文档（角色基底规范、20 个岗位总览、头像预览、命名规范）
- `preview/` — 20 张 400×400 缩略图
- `01_第一梯队_管理与总控组/` — 5 张
- `02_第二梯队_产品分析与设计组/` — 5 张
- `03_第三梯队_架构与研发组/` — 5 张
- `04_第四梯队_测试运维与文档组/` — 5 张

共 20 张 2048×2048 职位头像，文件名即职位名。
